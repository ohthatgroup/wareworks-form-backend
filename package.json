{
  "name": "wareworks-form-v2",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "cd apps/form-app && npm run dev",
    "build": "npm ci && cd apps/form-app && npm run build",
    "deploy": "netlify deploy --prod",
    "deploy:preview": "netlify deploy"
  },
  "devDependencies": {
    "@types/fontkit": "^2.0.8",
    "@types/node": "^20.19.9",
    "ts-node": "^10.9.2",
    "tsx": "^4.20.3",
    "typescript": "^5.2.0"
  },
  "dependencies": {
    "dotenv": "^17.2.1",
    "fontkit": "^2.0.4",
    "form-data": "^4.0.4",
    "mailgun.js": "^11.1.0",
    "pdf-lib": "^1.17.1"
  }
}
