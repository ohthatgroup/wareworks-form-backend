{
  "name": "wareworks-form-v2",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "cd apps/form-app && npm run dev",
    "build": "cd apps/form-app && npm run build",
    "deploy": "netlify deploy --prod",
    "deploy:preview": "netlify deploy"
  },
  "devDependencies": {
    "@types/node": "^20.19.9",
    "ts-node": "^10.9.2",
    "tsx": "^4.20.3",
    "typescript": "^5.2.0"
  },
  "dependencies": {
    "pdf-lib": "^1.17.1"
  }
}
